# Future work

- Publish a broker interoperability profile if more MCP hosts adopt delegated
  upstream credentials.
- Consider MCP URL elicitation only if automatic client-driven Monzo onboarding
  becomes a concrete requirement.
- Add a Docker MCP Catalog manifest and gateway-specific smoke test before
  claiming catalog compatibility.
- Review MCP SDK v2 after its stable release.
